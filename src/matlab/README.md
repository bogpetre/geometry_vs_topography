This folder contains prototyping scripts used to develop rdm_similarity.cpp.

These can be used in place of compiling rdm_similarity at the cost of ~2x the runtime.

See unit_test_rdm_cosim_scripts.m for a usage example.